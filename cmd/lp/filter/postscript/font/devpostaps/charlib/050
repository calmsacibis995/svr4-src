%ident	"@(#)lp:filter/postscript/font/devpostaps/charlib/050	1.1.2.1"
% Build file for ( (ASCII code 050).

/build_050 {
	pop
	gsave
	newpath
	0 0 moveto
	(\050) false charpath flattenpath pathbbox pop pop exch pop .50 mul neg
	grestore
	(\050) stringwidth pop .05 mul exch rmoveto
	(\050) show
} bind def
