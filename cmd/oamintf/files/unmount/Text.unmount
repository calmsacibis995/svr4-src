#ident	"@(#)filemgmt:unmount/Text.unmount	1.1.3.2"
Title="$ARG1"
close=`/bin/rm /tmp/umnt 2>/dev/null`CLOSE

`message "Press CANCEL to return to the previous frame."`

#help=OPEN TEXT $INTFBASE/Text.itemhelp unmounttask

begrow=distinct
begcol=distinct
rows=3
columns=55

text="`readfile $ARG2`"
