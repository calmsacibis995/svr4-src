#ident	"@(#)nadmin.nfs:local/nfs/Text.shmsg	1.1"
title=$ARG1
close=`echo "closing...";
unset -e RES -e DESC -e DIR -e ACC;close $FRM $FRM2`

`readfile "$INTFBASE/display.msg" | message -p`

text="`readfile $ARG2`"

