ABSTRACT:
	This task allows the administrator of a system to remove or
	install the default Spell Checker supplied with the FACE
	package.  When FACE is installed, the default Spell Checker
	is also installed.
